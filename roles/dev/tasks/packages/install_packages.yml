- name: packages | install required packages
  package:
    name:
      - "{{ docker_package }}"
      - "{{ docker_compose_package}}"
      - "{{ go_package }}"
      - "{{ npm_package }}"
      - "{{ php_package }}"
      - ripgrep
    state: latest
